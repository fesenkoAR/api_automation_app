const { Given, When, Then } = require('cucumber');

Given('I am logged in', function () {
  // Implementation to log in
});

When('I search for {string}', function (searchTerm) {
  // Implementation to perform search
});

When('I add the first result to my cart', function () {
  // Implementation to add item to cart
});

Then('the item should be added to my cart', function () {
  // Implementation to verify item is added to cart
});
